# Estado funcional del proyecto

Actualizado: 2026-09-15. Fuente: código, migraciones y pruebas del repositorio.

| Área | Estado | Implementación actual |
| --- | --- | --- |
| Agent Builder (fase 1 + fase 2 + Sprint 0 hardening) | Operativa en código, tras feature flag | `TenantAgent`/`TenantAgentVersion` son la fuente de verdad versionada; el binding se valida contra el registry de modelos. La UI `/voice-ai/agents` permite publicar y despublicar; despublicar abre un draft con toda la configuración editable de ServiGlobal, mientras los campos controlados por Ultravox siguen bloqueados en modo provider-managed. `PATCH /agents/{id}/draft` guarda identidad y versión en una transacción y la publicación exige el `expected_draft_version_id`. Se puede archivar y eliminar definitivamente un agente con historial: se eliminan agente y versiones, pero las sesiones y eventos conservan IDs históricos desvinculados. Para sesiones no terminales se cierra y verifica la sala LiveKit antes de cancelarlas; un dispatch en curso o un cierre no confirmado devuelve `409` y conserva el agente archivado. La UI usa `POST /agents/{id}/delete`; `DELETE` permanece compatible. Gateado por `agent_builder_v2`. |
| Voice Runtime Sprints 1–6 | WebRTC implementado en código; smoke real pendiente | `VoiceSession` fija la versión publicada, lifecycle e idempotencia; API interna con JWT corto; dispatch explícito LiveKit con metadata mínima; deploy independiente `voice-runtime` sin DB; token WebRTC corto, tenant-scoped y limitado a micrófono para que el navegador entre a la misma Room; CTA `Probar agente` en Agent Builder; detección de participante, transcript y salida de audio; cleanup y timeout de espera. `connected` ya no se emite al iniciar `AgentSession`, sino tras el primer transcript final. Las credenciales de proveedor de voz (Ultravox) ya no dependen de un `ULTRAVOX_API_KEY` global del contenedor `voice-runtime`: cada job las resuelve por `session_id` vía `GET /api/v1/internal/voice-runtime/sessions/{session_id}/credentials/{provider}`, que reutiliza `VoiceConfigService` sobre `TenantVoiceProviderConfig` (la config de Integraciones del tenant) y nunca las expone en `RuntimeSessionSpecV1`, LiveKit ni logs; dos tenants con claves Ultravox distintas quedan aislados por sesión. Gateado por `voice_runtime_v2`, deshabilitado por defecto. Ver `docs-local/fase-4/VOICE_RUNTIME_ARCHITECTURE.md` y `WEBRTC_VOICE_RUNTIME_SMOKE.md`. |
| Voice Runtime Sprint 7 — LiveKit SIP outbound | Implementado en código; PostgreSQL, LiveKit, PBX y audio real pendientes | `POST /api/v1/crm/leads/{lead_id}/actions/call` conserva el flujo Ultravox legacy por defecto. Con `voice_runtime_v2` y `livekit_sip_outbound_v2` habilitados, crea una `VoiceSession` SIP/outbound idempotente, despacha el runtime, espera `voice.agent.ready` y sólo entonces crea el participante SIP con el trunk LiveKit persistente del tenant. `LiveKitSipService` encapsula provisionamiento y marcado; `OutboundVoiceCallService` conserva la correlación `CrmVoiceCall`/`VoiceSession`/Room/participante/SIP call, aplica aislamiento tenant, países permitidos y capacidad, y cierra la Room ante fallo. Asterisk usa la plantilla neutral `serviglobal-tenant`; el alias legacy permanece para rollback. Pruebas locales usan fakes/mocks: no constituyen E2E ni prueban conectividad, Caller ID, media o audio bidireccional reales. |
| Voice Provider Abstraction (Fases A–E) | Implementada en código; UI y runtime completos, sin canary end-to-end con BYOK ElevenLabs real | `AgentVoiceConfig` (`mode: "provider"\|"provider_external"`) reemplaza la propiedad plana de voz sin romper agentes existentes: `provider` exige `voice.provider == realtime.provider`; `provider_external` sólo soporta `elevenlabs` hoy (`voice_registry.VoiceModel.external_voice_providers`, campo tipado, `capabilities` sigue siendo `dict[str, bool]` puro). Validación en dos capas: el schema valida forma/secretos, `VoiceSelectionService` valida reglas provider-specific (allowlist/rangos ElevenLabs) sin I/O — guardar un draft nunca llama a Ultravox. El runtime (`voice-runtime/providers.py`) traduce `mode="provider"` a `voice=<id>` y `mode="provider_external"` a `external_voice.elevenLabs` con un mapper explícito, con paridad de campos documentada y testeada contra el mapper backend (`build_elevenlabs_external_voice`, ambos sin código compartido entre procesos). `POST /integrations/voice/providers/ultravox/external-voice/preview` (roles de escritura) genera un preview real vía `POST /api/voice_preview` de Ultravox, un único intento sin retry, sin persistir nada. `AgentService.publish()` añade un preflight barato para `serviglobal_managed`: `provider` confirma con `UltravoxAdminService.get_voice` que la voz sigue accesible (incluye el `default_voice` legacy sintetizado sin migración); `provider_external` confirma vía `GET /accounts/me/tts_api_keys` que la cuenta Ultravox del tenant tiene BYOK de ElevenLabs, sin generar audio. `provider_managed` no se modifica ni valida voz. Agent Builder (`/voice-ai/agents`) expone un bloque "Origen de voz": catálogo del proveedor + Escuchar, o formulario ElevenLabs (voice_id, model libre, speed/stability/similarity_boost/speaker_boost) + Probar voz, con warning no bloqueante si no se probó antes de publicar; nada de esto se persiste. Ninguna credencial ElevenLabs propia en ServiGlobal en ningún punto del flujo. `UltravoxLegacyRuntimeAdapter` (código muerto sin callers desde antes de esta abstracción) se eliminó en la Fase F. Pendiente: canary real con BYOK ElevenLabs configurado en un tenant, y smoke Playwright de `/voice-ai/agents/new` (sesión de auth de este entorno expirada, ver `docs-local/fase-4/WEBRTC_VOICE_RUNTIME_SMOKE.md`). |
| Agent Builder V1 — Model Settings y Tools (Fases B–H) | Implementada en código; sin canary end-to-end con integraciones reales configuradas | Cierra dos brechas de `AGENT_BUILDER_ARCHITECTURE.md`: parámetros de modelo provider-aware y tools ejecutables de primera clase, ambos sobre `TenantAgentVersion.runtime_binding_json` (sin tablas nuevas). **Model settings**: `voice_registry.ParameterSpec` evoluciona (`type/step/options/advanced`); se corrige la inconsistencia real de `temperature` (`supported=False` pese a que `livekit-plugins-ultravox==1.7.1` y `voice-runtime/providers.py` ya lo soportan y validan 0–1) a `supported=True`; `max_duration` queda `supported=False` explícito (el runtime lo acepta sin validar tipo/rango propio, no se inventan límites). `voice_registry.validate_model_settings()` (puro, sin I/O) valida `realtime.settings` contra los parámetros declarados del modelo elegido; el Agent Builder renderiza un control dinámico sólo para parámetros `supported=true` (`AgentModelSection.tsx`), sin nada hardcodeado de Ultravox. **Tools**: nuevo `app.domain.tool_registry` (mismo patrón que `voice_registry`, sin DB/HTTP), inicialmente con 2 tools `status="available"` (`calendar.check_availability`, `whatsapp.send_message`) y 3 `status="planned"` (`calendar.create_booking`, `crm.create_lead`, `handoff.chatwoot`) porque `RuntimeSessionSpecV1.context` era siempre `{}` — brecha cerrada por Session Context / Contact Resolution V1 (ver fila siguiente), que reactivó `calendar.create_booking` y `crm.create_lead`; sólo `handoff.chatwoot` sigue `planned` por un bloqueo real no relacionado (`VoiceHandoffService` exige `TenantVoiceAgentConfig` legacy completo). `AgentToolBinding` (`{key, enabled, config}`, `config` inerte en V1) se valida y persiste en `runtime_binding_json["tools"]` (sibling top-level de `realtime`, nunca anidado — no colisiona con el `realtime.provider_extensions.tools` de solo lectura de agentes `provider_managed` importados). `_publish_tools_preflight` (rama `serviglobal_managed` únicamente) confirma por cada tool habilitada que su `required_integration` esté configurada, reutilizando la resolución real de cada servicio (`BookingService._effective_config`, `WhatsAppConfigService.get_active_client_config`). `GET /agents/tools/catalog` expone el catálogo anotado con disponibilidad por tenant. Ejecución real de punta a punta: `AgentCompilerService` resuelve los bindings en `CompiledToolSpec` (`key/name/description/input_schema`, nunca `config`) sobre un nuevo campo top-level `RuntimeSessionSpecV1.tools`; `voice-runtime/tool_dispatcher.py` registra cada uno como `RawFunctionTool` (`llm.function_tool`) en el `Agent(...)` de LiveKit (sólo para `serviglobal_managed`, nunca para `provider_managed`); el handler llama a `POST /api/v1/internal/voice-runtime/sessions/{id}/tools/{key}/invoke` (mismo trust boundary que `/spec` y `/events`) vía `ControlPlaneClient.invoke_tool`, un único intento sin retry (una tool puede tener efectos secundarios, ej. enviar un WhatsApp). El endpoint revalida el binding contra la versión publicada real en base de datos (nunca confía en lo que el runtime dice haber compilado), valida argumentos contra `input_schema` y despacha por un allowlist literal de handlers (`app.services.tool_dispatch_service`, sin `eval` ni import dinámico). Agent Builder gana pestañas "Modelo" (parámetros dinámicos, extraído de lo que antes vivía dentro de `VoiceFields`) y "Herramientas" (checkbox por tool `available`, banner "Requiere configurar X" con link si falta la integración; las `planned` nunca se listan). Errores de publish con código estable (`tool_not_found`, `tool_not_available`, `tool_integration_not_configured`, más los ya existentes de voz) se traducen a mensajes específicos en ES/EN en vez del genérico. Pendiente: canary con Cal.com/Google Calendar y WhatsApp reales configurados en un tenant, y el mismo smoke Playwright pendiente de Voice Provider Abstraction (sesión de auth expirada en este entorno). |
| Session Context / Contact Resolution V1 (Fases A–H, F.1) | Implementada en código; sólo cableada al flujo de prueba WebRTC, sin canary con llamadas reales | Capa transversal que resuelve identidad de negocio para una `VoiceSession` de forma segura y tenant-scoped, sin construir Batch Calling/CRM injection/Human Handoff completos. `RuntimeSessionSpecV1.context` pasa de `dict` genérico (siempre `{}` en producción — su único caller real, `GET .../sessions/{id}/spec`, nunca lo poblaba) a `SessionContextV1` tipado (`backend/app/schemas/session_context.py`, mirror manual en `voice-runtime/contracts.py`): `caller`/`contact`/`lead`/`campaign` mantenidos explícitamente separados (un caller es sólo la identidad telefónica de la llamada; un lead es un proceso comercial que pertenece a un contact) más `variables` de negocio limitadas (20 keys, 60 chars/key, profundidad 2, ~4KB) y sin secretos. `CampaignContext` no tiene `id` a propósito: no existe una entidad Campaign real en la plataforma (confirmado por auditoría), sólo el string libre `CrmLead.campaign`. Nuevo `ContactResolutionService` (único punto de resolución, no repartido entre servicios): precedencia `contact_id`/`lead_id` explícitos sólo si `trusted_ids=True` (nunca de la LLM) → lookup por teléfono normalizado (`voice_phone_service.normalize_caller_id`, no la normalización regex débil que puebla la mayoría de `CrmContact.phone_normalized` hoy — discrepancia de formato documentada, sin backfill) → sin resolver; **nunca crea** un Contact/Lead automáticamente (un caller desconocido queda `contact=null`, `lead=null` — crear sigue siendo una acción explícita). Migración mínima de una columna (`VoiceSession.session_context_json`, JSON nullable, sin tablas nuevas). Cableado únicamente al único punto real de creación de `VoiceSession` hoy: `POST /api/v1/voice/sessions` (flujo de prueba WebRTC, `WRITE_ROLES`), que ahora acepta `contact_id`/`lead_id`/`caller_phone` opcionales y confiables (nunca de un caller no autenticado); inbound SIP/PSTN real y un endpoint outbound dedicado no existen conectados a Agent Builder y quedan explícitamente fuera de este cierre (decisión confirmada con el usuario), documentados como bloqueados por infraestructura de originación de llamadas inexistente, no construida aquí. `ToolDispatchService` carga `SessionContextV1` desde la sesión y la pasa a los handlers — la identidad (`lead_id`/`contact_id`/`caller_phone`) nunca es un argumento que la LLM pueda controlar, sólo la intención de negocio (fecha, nombre, etc.) lo es. Con evidencia real (no asumida), esto reactivó 2 de las 3 tools que Agent Builder V1 dejó `planned`: `calendar.create_booking` (lead/contact vienen de `SessionContextV1`, falla cerrado con `lead_context_required` si no hay lead resuelto) y `crm.create_lead` (reutiliza `CrmContactService.get_or_create_contact` + `CrmLeadService.get_or_create_open_lead`, ya existentes — el teléfono viene de `session.caller.phone`, nunca de la LLM). `handoff.chatwoot` sigue `planned`: bloqueo real no relacionado (`VoiceHandoffService` exige un `TenantVoiceAgentConfig` legacy completo). Observabilidad: eventos `session.context.resolved/.contact_matched/.lead_matched/.unresolved/.tool_used` sobre `VoiceSessionEvent` (mismo patrón que `voice.session.*`), sin PII (sólo booleans/keys). **Fase F.1 — enriquecimiento controlado (monotónico)**: cerró una brecha funcional real — `crm.create_lead` resolvía/creaba un Contact/Lead pero nunca actualizaba `session.session_context_json`, así que un `calendar.create_booking` posterior en la misma sesión releía el contexto original sin resolver y fallaba con `lead_context_required`, rompiendo el flujo comercial esencial (caller desconocido → agente recopila datos → `crm.create_lead` → `calendar.create_booking`). `VoiceSessionService.enrich_context()` (nuevo) es la única vía de mutación de `SessionContextV1` tras la creación de la sesión: `unresolved → resolved` se permite; una identidad ya resuelta (`contact`/`lead`) nunca se reemplaza por otra — un conflicto de contacto o lead falla cerrado con `session_context_contact_conflict`/`session_context_lead_conflict`, y un tenant distinto con `session_context_tenant_conflict` (nada se persiste). `caller`/`campaign`/`variables`/`source` nunca se tocan; recibe filas ORM (`CrmContact`/`CrmLead`), nunca IDs sueltos, para poder validar `tenant_id` directamente. `ToolDispatchService._handle_create_lead` la invoca tras resolver el Contact/Lead (único punto de integración; `whatsapp.send_message`/`calendar.create_booking`/`calendar.check_availability` no enriquecen). Efecto observable: un `calendar.create_booking` en la misma sesión, después de un `crm.create_lead` exitoso, usa el `lead_id`/`contact_id` recién persistidos sin que el LLM los provea nunca (`input_schema` de ambas tools sigue sin `contact_id`/`lead_id`/`phone`) — cubierto por un test de encadenamiento real (`test_create_lead_then_create_booking_chains_the_enriched_lead_id_never_the_llms`). Evento `session.context.enriched` (mismo patrón sin PII). Deuda documentada, no resuelta en esta fase: normalización histórica de `CrmContact.phone_normalized` (sigue con la función regex débil, sin backfill) y semántica de destino de `whatsapp.send_message` (hoy siempre requiere `to_phone` explícito del LLM; separar "enviar al caller/contact actual" de "enviar a un destinatario explícito" queda como diseño futuro, no implementado). Pendiente: inbound/outbound reales, canary con una llamada real de punta a punta, smoke Playwright (mismo bloqueo de sesión Auth0 expirada que las demás fases). |
| Administración Ultravox tenant-scoped | Implementada en código; canary A/B real pendiente | Catálogos privados de agentes y voces, preview autenticado, importación y vínculo versionado. `provider_managed` persiste la referencia sólo en `TenantAgentVersion.runtime_binding_json`, revalida revisión/tools antes de cada call y bloquea tools cliente sin mapping. `ultravox-v0.7` es un alias lógico. El CallFactory aplica allowlist y diferencia `call_creation_failed` de `call_creation_outcome_unknown` sin reintentar POST. Importar crea un agente independiente; vincular conserva Ultravox como fuente de verdad. No es E2E hasta completar el canary real con dos tenants. |
| Centro de administración de proveedores de voz | Implementado en código; un único proveedor real | `Integraciones → Voz` renderiza `VoiceProviderAdminWorkspace` (pestañas Conexión/Agentes/Voces/Estado) parametrizado por `provider`, y el backend enruta `/integrations/voice/providers/{provider}/...` a través de `voice_provider_admin.get_provider_admin_service`, que despacha por `provider_key` contra `voice_registry.py` y sólo resuelve proveedores `status="active"` con adapter registrado (hoy solo `ultravox` → `UltravoxAdminService`). Sumar un proveedor real (OpenAI, Gemini, AWS Nova Sonic, ElevenLabs, Cartesia, Deepgram, ...) es: escribir su adapter, registrarlo en `_ADAPTERS`, marcarlo `status="active"` en el registry y pasar su `provider` a `VoiceProviderAdminWorkspace`; no requiere reconstruir Agent Builder ni el workspace. `VoiceIntegrationCard` (credenciales/SIP/agentes legacy) queda fuera de este alcance: su copy sigue asumiendo Ultravox y se generalizará cuando exista un segundo proveedor real que lo requiera. |
| Landing e i18n | Operativa | Landing bilingüe, pricing, demos, formularios y contenido SEO. |
| Auth0 y tenants | Operativa | Login, contexto privado, onboarding, administración, membresías y agentes. El administrador de tenants usa un catálogo compacto de integraciones y carga cada configuración en una ruta independiente. |
| Inicio (`/dashboard`) | Operativa | Resumen ejecutivo del tenant: bloque CRM (leads nuevos/calificados, citas, conversión), bloque Voz IA (llamadas, atendidas, tasa de respuesta, minutos) y "Atención requerida" (tareas vencidas, % de minutos usados, capacidad SIP, integraciones con error), todo desde endpoints ya existentes. Ya no redirige a `/crm`. |
| CRM | Operativa | Dominio puramente comercial. `/crm` es un resumen compacto (leads, tareas, próxima acción); `/crm/analytics` (antes `/crm/dashboard`) tiene pipeline, leads, detalle, timeline, notas, tareas, funnel, conversión, fuentes y campañas — ya sin `CallsPanel`/`VoiceCapacityPanel`. |
| Voz IA | Operativa | Dominio nuevo bajo `/voice-ai`: `experiences` (antes `crm/settings/voice-experiences`), `calls` (listado de llamadas recientes), `analytics` (KPIs, tendencias, distribuciones, heatmap, uso, ahorro y rendimiento de llamadas — antes en `/crm`) y `telephony` (capacidad SIP saliente, antes embebida en el dashboard CRM). |
| Integraciones | Operativa | Catálogo bilingüe de integraciones con búsqueda, filtros, estados resumidos y configuración por ruta, ahora transversal en `/integrations` (antes `crm/settings/integrations`). |
| Ingesta Ultravox | Operativa | Eventos, llamadas, estados, resumen, costos y correlación CRM. |
| Resend | Operativa | Configuración tenant, prueba, templates, preview, envío y trazabilidad. |
| Composer y assets | Operativa | Markdown/MDX seguro, resumen de llamada, adjuntos local/S3 y formularios públicos. |
| Cal.com (First-Class Provider) | Operativa | Proveedor de scheduling de primera clase vía API v2 centralizada (`CALCOM_API_VERSIONS`). Incluye descubrimiento de cuenta (`/v2/me`), sincronización y proyección de horarios, tipos de eventos, equipos y membresías, entidades proyectadas (`TenantSchedulingSchedule`, `TenantSchedulingEventType`, `TenantSchedulingProviderObject`), conciliación y marcado `remote_deleted` para entidades eliminadas remotamente sin rotura referencial, auditoría de eventos de integración, resolución de proveedores desacoplada vía `SchedulingProviderResolver`, resolución automática de `event_type_id` en reservas de agentes de voz (`create_lead_booking`), endpoints de sincronización y descubrimiento, UI de configuración simplificada con sección avanzada colapsable para compatibilidad con parámetros legados, tarjeta con resumen de cuenta descubierta y módulo `/agenda` unificado con pestaña de "Tipos de Cita", gestión de perfiles de disponibilidad y asignación directa de agentes IA a tipos de evento. |
| Google Calendar & Agenda | Operativa | Motor completo de scheduling multi-tenant desacoplado: OAuth multi-tenant centralizado, sincronización de calendarios, recursos independientes (asesores, médicos, salas), equipos y distribución Round Robin estricta (sin fallback a recursos ocupados), horarios semanales con jornadas partidas, buffers antes/después separados, excepciones/festivos, asignación a agentes de IA y módulo UI `/agenda`. |
| WhatsApp Cloud | Operativa en código | Configuración, envío CRM, mensajes, estados, webhook y ciclo de vida completo de plantillas. Flow Studio V1 añade builder visual versionado, generación desde Context Schema, preview, compilación determinista a Flow JSON, creación/upload/validación/publicación en Meta y clonación de versiones publicadas. Botones CTA: `QUICK_REPLY|URL|PHONE_NUMBER|VOICE_CALL|FLOW`; `VOICE_CALL` gateado por el feature grant `whatsapp_business_calling` (hoy sólo `platform_admin` vía API, sin UI). |
| Automatizaciones y notificaciones | Operativa en código | Contratos versionados de eventos, builder dinámico, dry-run sin envío, reglas, destinatarios, entregas, planificación, reintentos, recuperación y worker PostgreSQL. El despliegue del proceso worker debe verificarse por entorno. |
| Voz CRM | Operativa en código | El modal de llamada saliente lista `TenantAgent` activos con versión publicada desde `/api/v1/agents` y envía `agent_id`, `idempotency_key` UUID y `to_phone` al endpoint CRM; ya no selecciona un `TenantVoiceAgentConfig` legacy. El backend conserva el flujo SIP Ultravox legacy por feature flag; con V2 apagado, el agente canónico seleccionado no se usa y el backend elige su configuración legacy predeterminada. También incluye rutas SIP cifradas por tenant, aprovisionamiento PJSIP, llamadas WebRTC y callbacks salientes mediante IDT, webhook y booking tools. El agente local del PBX debe instalarse por entorno. |
| Voice Context Experiences | Runtime público WebRTC implementado en código | Feature flag, snapshots, submissions y context session one-shot; launch Ultravox tenant-scoped con recovery-first, leases, recovery por `joined`/`ended`, webhook firmado/deduplicado, billing real por `billedDuration`, CRM monotónico, concurrencia PostgreSQL y preflight de micrófono. `submissions=true`; `calls=true`. Ver `docs-local/fase-4/VOICE_EXPERIENCE_WEBRTC_RUNTIME.md`. |
| Planes y consumo | Operativa | Límites, alertas, resumen administrativo y comparación de ahorro. |
| Chatwoot | Multi-tenant, "external" y "managed" (Sprint 2 en curso) | Una Account de Chatwoot por tenant vía `TenantChatwootConfig`/`TenantChatwootInbox`, token cifrado, catálogo/config/test/provision en `Settings → Integrations → Chatwoot`. Modo "external" (formulario manual, sin cambios) y modo "managed": `POST /api/v1/integrations/chatwoot/provision` crea, vía la Platform API de Chatwoot, una Account + un usuario `administrator` dedicado (no un Agent Bot — verificado contra la instancia real que los tokens de Agent Bot no tienen permiso ni para crear un contacto) + un inbox `api` + el webhook de cuenta (mismo mecanismo que el operador configura a mano en modo "external"); requiere `CHATWOOT_PLATFORM_API_TOKEN` y `BACKEND_PUBLIC_BASE_URL`. Un fallo antes de crear la Account no persiste nada; uno posterior guarda `status="error"` conservando el `account_id` ya creado para diagnóstico. Validado end-to-end contra la instancia real el 2026-09-02. Webhook tenant-aware en `POST /api/v1/webhooks/chatwoot/{webhook_key}` (resuelve tenant por key, valida `account_id`); ya no responde automáticamente a mensajes entrantes (placeholder retirado). El motor legacy global (`chatwoot_service`) fue eliminado. `sanitize_chatwoot_error` reemplaza cualquier respuesta HTML (proxy, 404, mantenimiento) por un mensaje genérico antes de mostrarla en la UI. Vínculo a agentes de voz queda pendiente. |

## Voice Provider Abstraction V1 — cierre técnico

**Status: technically complete.** El Control Plane exige ahora `settings.model` para ElevenLabs external antes de guardar un draft o generar preview; el preflight BYOK exige un `prefix` string no vacío sin conservarlo ni exponerlo. Las pruebas automáticas locales cubren paridad de validación, preview sin I/O ante entrada inválida y las rutas legacy. **Manual UI/UX and production smoke validation remains a post-deploy operator task**; no se declara E2E ni validación de audio real con esta evidencia.

Limitaciones aceptadas de V1: Agent Builder carga sólo la primera página de hasta 50 voces Ultravox. Mejora futura: búsqueda asíncrona del catálogo, paginación/cursor y debounce, sin depender de las primeras 50. El preview WAV viaja hoy como Base64 por Server Actions; mejora futura: streaming de audio/blob mediante ruta autenticada. El puente legacy `settings.voice` y `default_voice` permanecen para compatibilidad; retirar `settings.voice` es candidato de una limpieza posterior, únicamente tras el smoke manual de agentes antiguos.

## Persistencia

Las migraciones cubren identidad, analítica, CRM, integraciones, Agent Builder (`tenant_agents`, `tenant_agent_versions`) y Voice Runtime (`voice_sessions`, `voice_session_events`). La migración más reciente es `202609140002_livekit_sip_outbound.py`, sobre `202609140001`: añade el trunk LiveKit persistente a la ruta SIP y la correlación SIP/CRM/runtime a `voice_sessions`, permitiendo que la configuración legacy de proveedor sea opcional sin perder la ruta tenant. La cadena conserva una única head. La migración aún requiere validación contra PostgreSQL real antes de desplegar; SQLite no demuestra este `ALTER` de constraint.

## Arquitectura de navegación del tenant

El shell dejó de identificarse como "CRM" (antes `CrmShell`/`CrmSidebar`/`CrmNavigation`) y pasó a `TenantShell`/`TenantSidebar`/`TenantNavigation` (`frontend/components/tenant/shell/`), compartido por todos los dominios vía el route group `frontend/app/[locale]/(tenant)/` (sin segmento en la URL). El encabezado del sidebar muestra "ServiGlobal IA" y el nombre del tenant en vez de "ServiGlobal / CRM".

```
Tenant
├── Inicio            /dashboard
├── CRM
│   ├── (resumen)      /crm
│   ├── Leads          /crm/leads
│   ├── Agenda         /agenda
│   ├── Tareas         /crm/tasks
│   └── Rendimiento    /crm/analytics
├── Voz IA
│   ├── Agentes        /voice-ai/agents
│   ├── Experiencias   /voice-ai/experiences
│   ├── Llamadas       /voice-ai/calls
│   ├── Analítica      /voice-ai/analytics
│   └── Telefonía      /voice-ai/telephony
├── Automatización
│   └── Notificaciones /automations/notifications
└── Integraciones      /integrations
```

Redirects permanentes desde las rutas legacy (preservan query params):

| Ruta anterior | Ruta nueva |
| --- | --- |
| `/dashboard` (redirigía a `/crm`) | Home real, ya no redirige |
| `/crm/dashboard` | `/crm/analytics` |
| `/crm/settings/integrations` y cualquier subruta | `/integrations` y la misma subruta |
| `/crm/settings/notifications` | `/automations/notifications` |
| `/crm/settings/voice-experiences`, `/new`, `/{id}` | `/voice-ai/experiences`, `/new`, `/{id}` |

"Configuración / Organización" no aparece todavía en el sidebar: no existe una pantalla de autoservicio de tenant (sólo `/admin/tenants/{id}` de plataforma), y el criterio del refactor es no introducir enlaces muertos.

## Continuidad: automatizaciones y notificaciones

- La página tenant es `frontend/app/[locale]/(tenant)/automations/notifications/page.tsx` (antes `crm/settings/notifications/page.tsx`, que ahora sólo redirige) y carga resumen, catálogo, capacidades, reglas, destinatarios, entregas y plantillas WhatsApp en paralelo.
- `NotificationsWorkspace` organiza cuatro pestañas: resumen, reglas, destinatarios y entregas. Los roles `platform_admin` y `tenant_admin` pueden escribir; analistas y viewers sólo consultan.
- Las reglas WhatsApp sólo pueden crearse con plantillas en `status="approved"`, ya sea importadas por sync desde Meta (`source="meta_sync"`, variables `POSITIONAL`) o creadas en la app y aprobadas (`source="tenant_authored"`, variables `NAMED`). El registro central relaciona capacidad/evento con campos tipados, operadores, formatos, rutas de destinatario, ejemplo seguro y versión.
- Las dos plantillas semilla (`lead_follow_up`, `meeting_reminder`) ya no aparecen como "activas" por defecto: inician como `draft` y deben enviarse a Meta y aprobarse antes de poder usarse en una regla o envío de plantilla real.
- El modal carga eventos según capacidad, genera condiciones y variables desde el contrato, conserva `all`/`any`, detecta rutas obsoletas y ofrece un dry-run que no crea entregas ni envía WhatsApp.
- Todos los campos del formulario de reglas tienen ayuda contextual bilingüe. `FieldHelp` abre desde el ícono de ayuda y cierra tanto al pulsarlo nuevamente como al hacer clic fuera.
- Los diálogos largos conservan tres zonas: encabezado, cuerpo desplazable y footer separado; no volver a introducir footers sticky dentro del área desplazable.
- El backend persiste `tenant_capabilities`, `tenant_notification_rules`, `tenant_notification_recipients`, `domain_events` y `notification_deliveries`. La planificación es idempotente y el worker usa claims con lease, reintentos, recuperación y estados terminales.
- Los commits `caed429` y `dbcdc8e` sólo cambiaron UI, traducciones y pruebas; no modificaron contratos backend, secretos ni resolución de tenant.

## Pendientes explícitos

- Agent Builder: backlog completo documentado en `docs-local/fase-4/AGENT_BUILDER_ARCHITECTURE.md` ("Pendientes"). **Actualizado por el cierre "Model Settings y Tools" (Fases B–H)**: `DynamicModelSettings` editable y la pestaña "Herramientas" ya existen (ver fila de tabla arriba) — el registro de `docs-local/` que los lista como "ni UI ni backend" quedó desactualizado por ese cierre, `docs/PROJECT_STATUS.md` prevalece. Siguen pendientes: `AgentRuntimeBinding` como entidad propia, Registry en base de datos, pestañas no construidas (Conocimiento, Flujos, Transferencias, Telefonía, Evaluaciones — ni UI ni backend, no confundir con "próximamente" ficticio), tools `status="planned"` (`calendar.create_booking`, `crm.create_lead`, `handoff.chatwoot`) sin resolución de lead/contacto por sesión para poder ejecutarse, sin `agent_version_id` en `CrmVoiceCall` ni en ninguna llamada real (`VoiceClient`/Ultravox siguen siendo el único runtime operativo), evento `runtime_binding_changed` no separado de `agent_draft_updated`, sin spec Playwright ni prueba manual en navegador para el flujo de tools, y una limitación conocida del backfill (no pasa por `validate_runtime_selection`).
- Habilitar Google Calendar `events.insert` sólo en un sprint dedicado con callback, scopes y pruebas completas.
- Evolucionar el builder de formularios y la UI de submissions si el producto lo requiere.
- Mantener separados futuros cambios de WhatsApp y voz.
- WhatsApp: sin webhook `message_template_status_update` de Meta en este MVP; el cambio de estado de una plantilla enviada sólo se refleja con el botón manual "Sincronizar". Fast-follow natural: suscribir `whatsapp_webhook.py` a ese campo.
- Flow Studio V1 administra definiciones estáticas y su ciclo de publicación, pero no ingiere respuestas `nfm_reply`, no crea `context_session`, no implementa Data Exchange/endpoint encryption y no conecta Flows con WhatsApp Calling, SIP, Asterisk o Ultravox.
- Fase B (Voz) del Template Management aún no arranca: vínculo agente de voz ↔ plantilla WhatsApp con mapeo de variables, y la nueva Voice Tool para disparar el envío en medio de una llamada. Se ramifica desde `develop` sólo después de mergear la Fase A (backend/frontend de creación, envío y sync de plantillas).
- El botón `VOICE_CALL` de un template sólo controla si Meta ofrece la llamada nativa de WhatsApp Business Calling; no dispara ni enruta llamadas hacia los agentes de voz/SIP internos (Sandra, Asterisk, Ultravox). Vincular ese CTA a un agente de voz vía SIP sigue sin diseñarse ni implementarse — mantiene la separación WhatsApp/voz de la línea anterior.
- Verificar por entorno los prerequisitos Ultravox tenant (API key, webhook secret, agente compatible con `user_context` y eventos webhook) antes de habilitar el runtime público.
- Etapa 0 (`fix/voice-experience-functional-alignment`): `get_current_published_version()` es fail-closed por `published_version_id`; `PUT` bloquea cambio de agente con historial; `DELETE` elimina definitivamente experiencias archivadas junto con versiones, submissions, valores, sesiones y runtime asociados; `/api/v1/calls` quedó tipado y sanitizado. Pendiente: endurecer `/api/v1/call-outbound` con el mismo criterio.
- Callback saliente multitenant: cada tenant configura una ruta SIP cifrada y Caller ID autorizado; el backend genera el usuario SIP estable `route-<uuid>` para que coincida con el endpoint PJSIP y la interfaz lo mantiene en solo lectura. Las experiencias publicadas pueden solicitar una llamada idempotente usando sólo el token de contexto. El backend normaliza E.164 para CO, MX, AR, PA, CL, EC, PE y US, y el worker inicia Ultravox con la credencial SIP del tenant. `call.ended` libera inmediatamente la capacidad y el worker reconcilia con Ultravox los estados activos cuando falta el webhook, con cierre máximo de seguridad configurable. El dashboard muestra ocupación, cupos, saturaciones y liberaciones con eventos sanitizados por tenant; sólo administradores enlazan a la configuración, mientras analyst/viewer permanecen en lectura. El agente local de Asterisk reconcilia un include PJSIP administrado y reporta cada revisión; una ruta pendiente o fallida no puede originar llamadas. WebRTC permanece independiente.
- El aviso de cambios sin guardar cubre recarga/cierre y navegación mediante enlaces; el historial nativo atrás/adelante sigue como limitación conocida de App Router hasta disponer de un hook estable.
- El listado consulta el historial por experiencia para mostrar su conteo. Si `max_experiences` crece y esto se vuelve un cuello de botella, el backend deberá incluir el conteo en la respuesta del listado.
- El inventario de Voice Experiences muestra las experiencias publicadas como listas para compartir y ofrece abrir o copiar su enlace público desde cada tarjeta; el enlace usa siempre `experience.default_locale`, no el idioma que esté navegando el administrador.
- El tema de una experiencia admite `background_color` y `color_scheme` (`light`/`dark`, `light` por defecto) además de `logo_url`, `primary_color` y `layout`; se versiona con la publicación y se renderiza igual en la vista previa del editor y en el formulario público mediante `resolveVoiceTheme`.
- Cada experiencia publicada expone `/{locale}/voice/{slug}/embed`, la misma página pública sin cabecera de sitio ni márgenes de página completa, pensada para `<iframe>`; comunica su altura al padre vía `postMessage` (`ResizeObserver`) y sólo esa ruta declara `Content-Security-Policy: frame-ancestors *`. El panel "Compartir / Incrustar" del listado genera enlace, y fragmentos de código (HTML/React/iframe) para incrustación inline, botón flotante o modal, usando el SDK vanilla `frontend/public/voice-embed.v1.js` (`window.VoiceEmbed`). La configuración de texto/posición del botón flotante y el selector del disparador del modal son sólo del fragmento generado; no se persisten en backend.
- El editor permite seleccionar snapshots históricos para previsualizarlos, restaurarlos como borrador y eliminar cualquier versión sin referencias que no sea la publicada actualmente; las versiones referenciadas, publicadas o pertenecientes a experiencias archivadas permanecen protegidas.
- En Agente y contexto, las versiones de esquema se pueden abrir desde el historial; las versiones activas o archivadas se editan mediante un borrador nuevo o existente. Abrir/crear el borrador es idempotente, lo selecciona automáticamente y evita conflictos cuando ya existe otra versión draft.
- Confirmar que cada entorno con automatizaciones tenga un proceso persistente `python -m app.workers.notification_worker` conectado a PostgreSQL.
- Renovar la sesión Playwright con `npm.cmd run qa:auth` antes de la validación visual si la prueba redirige a la pantalla de login.
- Validar en cada entorno credenciales, URLs públicas, webhooks, CORS y almacenamiento; no asumir que los defaults locales representan producción.
- El contenido propio de `/dashboard`, `/crm` y los encabezados de `/voice-ai/*` es bilingüe vía `next-intl` (`crm.tenantHome`, `crm.commercialHome`, `crm.voiceAi`, `crm.analyticsLoadError` en `messages/{es,en}.json`), igual que `crm.navigation`. `crm-dashboard-view-client.tsx` (la vista "Rendimiento" de `/crm/analytics`, movida pero no reescrita en este refactor) y los componentes de `components/dashboard/*` (`KpiCards`, `TrendsChart`, etc.) siguen con texto en español embebido, como ya estaba antes de este refactor; extender i18n ahí es un esfuerzo aparte.
- `frontend/tests/tenant-shell.spec.ts` cubre que `/dashboard` no redirige a `/crm`, los redirects legacy, el sidebar por dominio y que `crm/analytics` ya no muestra `CallsPanel`/`VoiceCapacityPanel`; corre en el proyecto `crm-visual` y necesita la misma sesión Auth0 renovada.

## Evidencia de pruebas

El repositorio contiene cobertura backend específica para identidad, analítica, CRM, dashboard, Resend, assets, formularios, Cal.com, Google Calendar foundation, WhatsApp, voz, context schemas, Voice Experiences, resolución pública y snapshots publicados, webhooks, límites de uso, el pipeline de notificaciones y Agent Builder. Voice Runtime añade pruebas del token LiveKit, grants, aislamiento tenant, lifecycle/eventos, resolución de credenciales por tenant (incluye aislamiento cross-tenant de la API key) y del runtime standalone; `frontend/tests/livekit-adapter.spec.ts` cubre conexión, publicación única de micrófono y cleanup. El frontend incluye lint, typecheck, build y pruebas privadas/públicas existentes. La prueba WebRTC bidireccional con LiveKit y Ultravox reales sigue siendo una verificación de entorno, no una prueba unitaria; usar `docs-local/fase-4/WEBRTC_VOICE_RUNTIME_SMOKE.md`. Los resultados históricos están en `docs-local/` y deben ejecutarse nuevamente antes de cada entrega.
